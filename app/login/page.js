import React from 'react';
import LoginPage from '@/components/Login';

export const metadata = {
    title: 'Login | CodesCloth',
}

const Login = () => {
    
    return (
        <>
            <LoginPage />
        </>
    )
}

export default Login
